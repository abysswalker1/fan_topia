import { Fancybox } from "@fancyapps/ui";

(function () {
  // Fancybox.show([{ src: "#dialog-content", type: "inline" }]);
  // Fancybox.show([{ src: "#verify-account-modal", type: "inline" }]);
})();
